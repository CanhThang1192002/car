## FE (build-time)
# Public URL để trình duyệt gọi API (nên thay bằng domain/IP public của server bạn)
# Dùng cùng origin với web (nginx sẽ proxy /api sang BE)
PUBLIC_API_BASE_URL=https://carweb-api.34.142.180.166.nip.io
WEB_PORT=8081

## BE -> SQL Server (runtime)
DB_HOST=100.93.2.3
DB_PORT=1433
DB_NAME=oto
DB_USER=sa
DB_PASSWORD=Admin@123

